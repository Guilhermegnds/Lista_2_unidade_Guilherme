#include <stdio.h>
#include <math.h>

int main(){
    int l, x, a;
    printf("Digite um numero qualquer: ");
    scanf("%d", &l);
    a=0;
    for (x=0; x<=sqrt(l); x++) {
        a=x*x;

          }
    printf("\n*%d*\n\n", x);
    return 0;
}
